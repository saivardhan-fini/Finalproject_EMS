@model EMS.Models.Grade_master

@{
    ViewBag.Title = "Create";
}

<h2 class="page-title">Create Grade Master</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-container">
        <h4 class="form-heading">Grade Master Details</h4>
        <hr class="form-separator" />

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            @Html.LabelFor(model => model.Grade_Code, htmlAttributes: new { @class = "form-label" })
            @Html.EditorFor(model => model.Grade_Code, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Grade_Code, "", new { @class = "text-danger" })
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Description, htmlAttributes: new { @class = "form-label" })
            @Html.EditorFor(model => model.Description, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Description, "", new { @class = "text-danger" })
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Min_Salary, htmlAttributes: new { @class = "form-label" })
            @Html.EditorFor(model => model.Min_Salary, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Min_Salary, "", new { @class = "text-danger" })
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Max_Salary, htmlAttributes: new { @class = "form-label" })
            @Html.EditorFor(model => model.Max_Salary, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Max_Salary, "", new { @class = "text-danger" })
        </div>

        <div class="form-actions">
            <input type="submit" value="Create" class="btn btn-primary" />
            <a href="@Url.Action("Index")" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
}

<style>
    body {
        font-family: 'Roboto', sans-serif;
        background-color: #f0f0f0; /* Light gray background for the whole page */
        color: #333;
        margin: 0;
        padding: 0;
    }

    .page-title {
        text-align: center;
        color: #007bff; /* Dodger Blue for headings */
        margin-bottom: 30px;
        font-size: 2rem;
        font-weight: 700;
    }

    .form-container {
        max-width: 600px; /* Match the max-width of Department form */
        margin: 0 auto;
        padding: 20px;
        background-color: #333; /* Dark background to match Department form */
        border: 1px solid #ddd; /* Light gray border */
        border-radius: 12px; /* Rounded corners */
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.5); /* Consistent shadow */
    }

    .form-heading {
        color: #007bff; /* Dodger Blue for subheadings */
        font-size: 1.5rem;
        margin-bottom: 15px;
    }

    .form-separator {
        border: none;
        border-top: 2px solid #007bff; /* Dodger Blue for separator */
        margin-bottom: 20px;
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-label {
        display: block;
        margin-bottom: 8px;
        font-weight: 500;
        color: #ddd; /* Light gray for labels */
    }

    .form-control {
        width: 100%;
        padding: 10px;
        border: 1px solid #007bff; /* Dodger Blue border for input fields */
        border-radius: 6px;
        font-size: 1rem;
        color: #ffffff; /* White text color */
        background-color: #333; /* Dark background for input fields */
    }

    .btn-primary {
        background-color: #007bff; /* Dodger Blue for button */
        color: #ffffff;
        border: none;
        border-radius: 6px;
        padding: 14px;
        font-size: 1.1rem;
        font-weight: 700;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

        .btn-primary:hover {
            background-color: #0056b3; /* Darker blue for hover effect */
            transform: scale(1.02);
        }

    .btn-secondary {
        background-color: #6c757d; /* Secondary color for back button */
        color: #ffffff;
        border: none;
        border-radius: 6px;
        padding: 14px;
        font-size: 1.1rem;
        font-weight: 700;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

        .btn-secondary:hover {
            background-color: #5a6268; /* Darker shade for hover */
            transform: scale(1.02);
        }

    .text-danger {
        color: #dc3545; /* Danger color for error messages */
    }

    .form-actions {
        display: flex;
        justify-content: space-between;
        margin-top: 20px;
    }
</style>
